{
  "name": "learn-javascript",
  "version": "1.0.0",
  "author": "Yaffleria",
  "scripts": {
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "nodemon": "^3.0.2",
    "typescript": "^5.3.2"
  }
}
